// MyTheme.js
import {extendTheme} from 'native-base';

const MyTheme = extendTheme({
  fonts: {
    body: 'VinaSans-Regular',
    heading: 'VinaSans-Regular',
    // Add more font styles if necessary
  },
});

export default MyTheme;
